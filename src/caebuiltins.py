import caeerr

class Value:

    def mul(self):
        caeerr.throw(caeerr.UnsupportedOpError, "Unsupported Operation")

    def div(self):
        caeerr.throw(caeerr.UnsupportedOpError, "Unsupported Operation")

    def plus(self):
        caeerr.throw(caeerr.UnsupportedOpError, "Unsupported Operation")

    def minus(self):
        caeerr.throw(caeerr.UnsupportedOpError, "Unsupported Operation")
    
